
# Language setting
language: clojure

# Version number
lein:
  - lein2

build:
  ci:
    - lein version
   # - mkdir -p shippable/testresults
    #- mkdir -p shippable/codecoverage
    - cd sample  
    - lein test2junit
    #- timeout 5#to fail this build
   
    
integrations:
    notifications:
        - integrationName: ship-hipchat
          type: hipchat
          recipients:
           # - '#hitchcock1'
            - '#hitchcock1pvt'
            - '@shiphitchcock1'
        #  on_start: never
         # on_success: change
          #on_failure: change
          #on_pull_request: never 
